# node
node_modules/
npm-debug.log*
yarn-error.log*

# tasks em andamento — nao versionar (apenas approved/ e archive/ sao fonte de verdade)
outputs/*
!outputs/approved/
!outputs/archive/
!outputs/approved/**
!outputs/archive/**
!outputs/INDEX.md

# videos de referencia (binarios grandes > limite do GitHub) — manter so localmente
assets/reference-videos/

# acervo de imagens enviadas pelo usuario (runtime, nao versionar)
interface/public/uploads/

# dados de acesso do painel: users.json (hashes de senha) + .session_secret — NUNCA versionar
interface/data/

# logs e temp
logs/
*.log
*.tmp
*.tmp.*

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code (dados de sessao locais) — nunca versionar
.claude/

# Certificado de origem do Cloudflare (fica só no servidor).
# A chave privada estava fora do git E fora daqui: um `git add -A` distraído
# commitaria caddy/origin/key.pem no repositório. Nunca versionar.
caddy/origin/
*.pem

# rascunhos de layout da peca de Midia e propostas visuais (nunca foram para o painel)
interface/_mediatest/
interface/public/propostas/
interface/public/propostas.html
interface/public/outputs/
