mkt.4st.co {
	encode gzip

	# TLS com o Cloudflare Origin Certificate (Cloudflare em modo Full/strict via Page Rule).
	tls /etc/caddy/origin/cert.pem /etc/caddy/origin/key.pem

	# SEM basic_auth: a autenticacao e o login do proprio painel (interface/lib/auth.js).
	# (Removido em 2026-07-10 — antes havia um porteiro compartilhado "equipe".)
	reverse_proxy panel:4500
}

# >>> AI-SQUAD (radar-web) — gerenciado pelo Claude — remover este bloco reverte
squad.4st.co {
	tls /etc/caddy/origin/cert.pem /etc/caddy/origin/key.pem
	reverse_proxy radar-web:3000
}
# <<< AI-SQUAD
